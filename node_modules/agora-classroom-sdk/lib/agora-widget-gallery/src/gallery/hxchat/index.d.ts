import { IAgoraWidget } from 'agora-edu-core';
export declare class AgoraHXChatWidget implements IAgoraWidget {
    widgetId: string;
    constructor();
    widgetDidLoad(dom: Element, props: any): void;
    widgetRoomPropertiesDidUpdate(properties: any, cause: any): void;
    widgetWillUnload(): void;
}
