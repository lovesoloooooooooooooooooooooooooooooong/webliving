export declare class HomeApi {
    static shared: HomeApi;
    domain: string;
    login(userUuid: string): Promise<{
        rtmToken: string;
        userUuid: string;
        appId: string;
    }>;
}
