import { BoardUIStore, EduClassroomStore, EduShareUIStore } from 'agora-edu-core';
export declare class LectureH5BoardUIStore extends BoardUIStore {
    heightRatio: number;
    aspectRatio: number;
    constructor(store: EduClassroomStore, shareUIStore: EduShareUIStore);
    borderZoomStatus: string;
    get iconBorderZoomType(): "zoom-out-gray" | "zoom-in-gray";
    get iconZoomVisibleCls(): "hidden" | "";
    get containerH5VisibleCls(): "hidden" | "";
    get whiteboardContainerCls(): "" | "flex-1";
    get rootDimensions(): {
        width: number;
        height: number;
    };
    get boardContainerHeight(): number | "unset";
    handleBoradZoomStatus(): void;
}
