import { CSSProperties, FC } from 'react';
import { IAgoraWidget } from 'agora-edu-core';
import './index.css';
interface BaseProps {
    style?: CSSProperties;
    className?: string;
    id?: string;
}
export interface WidgetProps extends BaseProps {
    widgetComponent: IAgoraWidget;
    widgetProps?: Record<string, unknown>;
}
export declare const Widget: FC<WidgetProps>;
export declare const WidgetOuter: FC;
export {};
