import { TooltipPlacement } from '../tooltip';
declare type StreamIconProps = {
    className?: string;
    tooltip?: string;
    size?: number;
    tooltipPlacement?: TooltipPlacement;
    iconType: string;
};
export declare const StreamIcon: ({ className, tooltip, tooltipPlacement, size, iconType, }: StreamIconProps) => JSX.Element;
export {};
