import { FC } from 'react';
import { BaseProps } from '~ui-kit/components/interface/base-props';
import './index.css';
declare type WindowId = string | number;
declare type WindowTitle = string | number;
interface WindowItem {
    id?: WindowId;
    title?: WindowTitle;
    image?: string;
}
export interface ScreenShareProps extends BaseProps {
    screenShareTitle?: string;
    scrollHeight?: number;
    windowItems?: WindowItem[];
    onActiveItem: (id: WindowId) => void;
    currentActiveId: WindowId;
}
export declare const ScreenShare: FC<ScreenShareProps>;
export {};
