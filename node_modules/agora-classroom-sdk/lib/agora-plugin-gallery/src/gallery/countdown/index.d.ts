import 'promise-polyfill/src/polyfill';
import { PluginStore } from './store';
import { IAgoraExtApp, AgoraExtAppContext, AgoraExtAppHandle } from 'agora-edu-core';
export declare class AgoraExtAppCountDown implements IAgoraExtApp {
    readonly language: any;
    static store: PluginStore;
    appIdentifier: string;
    appName: string;
    icon: string;
    width: number;
    height: number;
    minWidth: number;
    minHeight: number;
    store?: PluginStore;
    constructor(language?: any);
    extAppDidLoad(dom: Element, ctx: AgoraExtAppContext, handle: AgoraExtAppHandle): void;
    extAppRoomPropertiesDidUpdate(properties: any, cause: any): void;
    extAppWillUnload(): Promise<boolean>;
}
