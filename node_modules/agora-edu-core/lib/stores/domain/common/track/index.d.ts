import { EduClassroomStore } from '../..';
import { Track } from './struct';
import { Dimensions, Point, TrackAdapter, TrackContext } from './type';
import { EduStoreBase } from '../base';
export declare class TrackStore extends EduStoreBase {
    private _trackAdapter;
    private _posOnly?;
    trackById: Map<string, Track>;
    private _context?;
    constructor(store: EduClassroomStore, _trackAdapter: TrackAdapter, _posOnly?: boolean | undefined);
    setTrackContext(context: TrackContext): void;
    reposition(): void;
    setTrackById(trackId: string, end: boolean, pos: Point & {
        real: boolean;
    }, dimensions?: Dimensions & {
        real: boolean;
    }, options?: {
        needTransition?: boolean;
        initial?: boolean;
    }): void;
    updateTrack(trackId: string, position: Point, size: Dimensions, initial?: boolean): Promise<void>;
    setTrackLocalDimensionsById(trackId: string, dimensions: Dimensions & {
        real: boolean;
    }): void;
    deleteTrackById(trackId: string): void;
    private updateTrackMap;
    onDestroy(): void;
    onInstall(): void;
}
