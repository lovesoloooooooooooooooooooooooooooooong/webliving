import { EduClassroomStore } from '..';
import { TrackData, TrackState } from '../room/type';
import { TrackAdapter } from '../track/type';
export declare class ExtAppTrackAdapter implements TrackAdapter {
    private _classroomStore;
    constructor(_classroomStore: EduClassroomStore);
    updateTrackState(trackId: string, trackData: TrackData): void;
    deleteTrackState(trackId: string): void;
    get trackState(): TrackState;
}
