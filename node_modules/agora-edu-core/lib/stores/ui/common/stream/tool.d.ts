/**
 * 视频窗工具类型
 */
export declare enum EduStreamToolCategory {
    camera = 0,
    microphone = 1,
    whiteboard = 2,
    podium = 3,
    podium_all = 4,
    star = 5
}
/**
 * 视频窗工具对象
 */
export declare class EduStreamTool {
    iconType: string;
    hoverIconType?: string;
    style?: Record<string, string | number>;
    interactable: boolean;
    toolTip: string;
    category: EduStreamToolCategory;
    onClick?: () => void;
    constructor(category: EduStreamToolCategory, iconType: string, toolTip: string, options?: {
        style?: Record<string, string | number>;
        interactable?: boolean;
        onClick?: () => void;
        hoverIconType?: string;
    });
}
