import { EduUIStoreBase } from './base';
import { EduClassroomStore } from '../../domain';
import { EduShareUIStore } from './share-ui';
export declare class BoardUIStore extends EduUIStoreBase {
    private _timeoutForConfig;
    private _joinDisposer?;
    protected heightRatio: number;
    protected aspectRatio: number;
    private _collectorContainer;
    onInstall(): void;
    /**
     * 白板准备好挂载到 DOM
     * @returns
     */
    get readyToMount(): boolean;
    /**
     * 白板连接中断
     * @returns
     */
    get connectionLost(): boolean;
    /**
     * 白板高度
     * @returns
     */
    get boardHeight(): number;
    /**
     * 设置白板课件最小化 DOM
     */
    set setCollectorContainer(collectorContainer: HTMLElement);
    constructor(store: EduClassroomStore, shareUIStore: EduShareUIStore);
    /**
     * 等待白板配置就绪后连接白板
     */
    joinWhiteboardWhenConfigReady(): void;
    /**
     * 重连白板
     */
    rejoinWhiteboard(): Promise<void>;
    /**
     * 连接白板
     */
    joinWhiteboard(): Promise<void>;
    /**
     * 断开白板
     */
    leaveWhiteboard(): Promise<void>;
    /**
     * 白板挂载到 DOM
     * @param dom
     */
    mount(dom: HTMLDivElement): Promise<void>;
    /**
     * 白板卸载，销毁白板实例
     */
    unmount(): Promise<void>;
    onDestroy(): void;
}
