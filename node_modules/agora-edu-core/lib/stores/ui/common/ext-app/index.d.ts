import { EduUIStoreBase } from '../base';
import { IAgoraExtApp } from '../../../..';
export declare class ExtAppUIStore extends EduUIStoreBase {
    /** Getters  */
    /**
     * 当前用户是否拥有 ExtApp 的关闭权限
     * @returns
     */
    get canClose(): boolean;
    /**
     * 当前用户是否拥有 ExtApp 的移动权限
     * @returns
     */
    get canDrag(): boolean;
    /**
     * 当前打开的 ExtApp
     * @returns
     */
    get activeApps(): IAgoraExtApp[];
    /**
     * 更新 ExtApp 属性
     * @param appId
     * @returns
     */
    private handleUpdate;
    /**
     * 移除 ExtApp 属性
     * @param appId
     * @returns
     */
    private handleDelete;
    /**
     * 关闭 ExtApp
     * @param appId
     */
    shutdownApp(appId: string): void;
    /**
     * 启动 ExtApp
     * @param appId
     */
    launchApp(appId: string): void;
    /**
     * 挂载 ExtApp 到 DOM
     * @param dom
     * @param extApp
     */
    mount(dom: HTMLElement | null, extApp: IAgoraExtApp): void;
    /**
     * 更新 ExtApp 的轨迹信息
     * @param appId
     * @param width
     * @param height
     */
    updateTrackState(appId: string, width: number, height: number): void;
    /**
     * ExtApp 是否已启动
     * @param extApp
     * @returns
     */
    private isActive;
    /**
     * ExtApp 事件通知
     */
    private fireEvent;
    onInstall(): void;
    onDestroy(): void;
}
