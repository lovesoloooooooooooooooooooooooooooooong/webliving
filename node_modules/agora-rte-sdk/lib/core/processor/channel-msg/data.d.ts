import { AgoraChatMessage, AgoraRoom, AgoraStream, AgoraUser } from './struct';
import Immutable from 'immutable';
export declare class AgoraRteSyncDataStore {
    private _users;
    get users(): Immutable.Map<string, AgoraUser>;
    setUsers(users: Map<string, AgoraUser>): void;
    setUser(key: string, user: AgoraUser): void;
    deleteUser(key: string): void;
    findUser(userUuid: string): AgoraUser | undefined;
    private _roomProperties;
    get roomProperties(): Immutable.Map<string, unknown>;
    setRoomProperties(roomProperties: any): void;
    private _streams;
    get streams(): Immutable.Map<string, AgoraStream>;
    setStreams(streams: Map<string, AgoraStream>): void;
    findUserStreams(userUuid: string): AgoraStream[];
    setStream(key: string, stream: AgoraStream): void;
    deleteStream(key: string): void;
    findStream(streamUuid: string): AgoraStream | undefined;
    private _roomInfo;
    get roomInfo(): Immutable.Map<unknown, unknown>;
    setRoomInfo(roomInfo: AgoraRoom): void;
    private readonly _messages;
    get messages(): Immutable.List<AgoraChatMessage>;
    addMessage(message: AgoraChatMessage): void;
}
