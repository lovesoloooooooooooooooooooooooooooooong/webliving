import { AGRenderMode } from './type';
export declare class AgoraRtcVideoCanvas {
    streamUuid: string;
    channelName: string;
    view: HTMLElement;
    mirror?: boolean;
    renderMode: AGRenderMode;
    static key(streamUuid: string, channelName: string): string;
    constructor(streamUuid: string, channelName: string, view: HTMLElement, mirror?: boolean, opts?: {
        renderMode?: AGRenderMode;
    });
}
export declare class AgoraRtcLocalVideoCanvas extends AgoraRtcVideoCanvas {
    constructor(view: HTMLElement, mirror?: boolean);
}
