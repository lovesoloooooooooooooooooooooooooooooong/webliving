import { AgoraRteThread } from '../../../utils/thread';
import { AgoraRtcVideoCanvas } from '../../canvas';
import { AgoraRteMediaSourceState } from '../../type';
import { RtcAdapterElectron } from '.';
import { LOCAL_VIDEO_STREAM_STATE } from 'agora-electron-sdk/types/Api/native_type';
import { Injectable } from '../../../decorator/type';
export declare class AgoraRteElectronCameraThread extends AgoraRteThread {
    protected logger: Injectable.Logger;
    canvas?: AgoraRtcVideoCanvas;
    trackState: AgoraRteMediaSourceState;
    cameraEnable: boolean;
    private _adapter;
    currentCanvas?: AgoraRtcVideoCanvas;
    videoStreamState: LOCAL_VIDEO_STREAM_STATE;
    constructor(adapter: RtcAdapterElectron);
    private setCameraTrackState;
    onExecution(): Promise<void>;
}
