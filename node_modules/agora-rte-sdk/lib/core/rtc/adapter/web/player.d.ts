import { Injectable } from '../../../decorator/type';
import { AGEventEmitter } from '../../../utils/events';
declare global {
    interface Window {
        webkitAudioContext: AudioContext;
    }
}
export declare class AGWebAudioPlayer extends AGEventEmitter {
    protected logger: Injectable.Logger;
    private _audioContext?;
    private _audioTagId;
    private _playingSourceNode?;
    private _analyserNode?;
    private _volumePollingTask?;
    constructor();
    private _getAudioVirtualDom;
    play(url: string): void;
    stop(): void;
}
