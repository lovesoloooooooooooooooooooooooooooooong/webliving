import { PropsWithChildren } from "react";
interface Props {
    size: {
        width: number;
        height: number;
    };
    scale: number;
}
export declare function FlexTransform({ scale, size, children }: PropsWithChildren<Props>): JSX.Element;
export {};
