import type { AppContext, Player, Room } from "@netless/window-manager";
import { Component } from "react";
import type { Props, Attributes } from "../types";
export declare class MediaPlayer extends Component<Props> {
    render(): JSX.Element;
    putAttributes: (b: Partial<Attributes>) => void;
}
export interface ImplProps {
    room?: Room;
    player?: Player;
    context: AppContext<Attributes>;
    plugin: {
        putAttributes(attrs: any): void;
    };
}
